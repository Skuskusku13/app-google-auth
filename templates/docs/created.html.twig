{% extends 'base.html.twig' %}

{% block body %}
    <div style="padding: 50px; text-align: center;">
        <h1>✅ Document créé avec succès !</h1>

        <p><strong>Titre :</strong> {{ title }}</p>
        <p><strong>ID :</strong> {{ documentId }}</p>

        <br>
        <a href="{{ url }}" target="_blank" style="display: inline-block; padding: 12px 24px; background: #4285f4; color: white; text-decoration: none; border-radius: 5px; font-weight: 500;">
            📄 Ouvrir le document dans Google Docs
        </a>

        <br><br>
        <a href="{{ path('app_home') }}">Retour à l'accueil</a>
    </div>
{% endblock %}
